namespace SpaceTaxi_1.States {
    /// <summary>
    /// Sets a enum for all the different states.
    /// </summary>
    public enum GameStateType {
        GameRunning,
        GamePaused,
        MainMenu,
        Gamelevels,
        GameControls,
        GameOver,
        GameVictory
    }
}